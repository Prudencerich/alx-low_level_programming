#include "main.h"

/**
 * _islower - checks for lowercase character during execution of program.
 * 
 * @c: the character to check during execution of program.
 *
 * Return: 1 if c is lowercase, 0 otherwise in the program.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
