#include "main.h"
/**
 * _isupper - uppercase alphabet in program
 *
 * @c: chars to be check in program
 *
 * Return: NULL or ONE
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else;

	return (0);
}
