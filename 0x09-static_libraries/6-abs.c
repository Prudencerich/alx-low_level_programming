#include "main.h"


/**
 * _abs - computes the absolute value of an integer function in the program.
 *
 * @n: the int to check function in the program.
 *
 * Return: the absolute value of int value in the program.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
